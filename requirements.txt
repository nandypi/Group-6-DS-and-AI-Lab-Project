# FinQuery — All project dependencies (CPU-only)
#
# All machines are assumed to be CPU-only.
# The PyTorch CPU wheel index is included so a single command installs everything:
#   pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cpu

# -- Application runtime ------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.0
streamlit==1.61.1
PyJWT==2.3.0
requests==2.32.3
pydantic==2.11.4
openai==1.78.0
chromadb==1.5.9
tqdm==4.67.1
PyYAML==6.0.3
tiktoken==0.9.0
python-dotenv==1.1.0
rank-bm25==0.2.2

# -- Authentication -----------------------------------------------------------
passlib[bcrypt]>=1.7.4

# -- ML / benchmarking --------------------------------------------------------
torch==2.7.0+cpu
transformers==4.51.3
FlagEmbedding

# -- Evaluation (RAGAS benchmark) ---------------------------------------------
ragas==0.2.15
datasets
langchain==0.3.27
langchain-community==0.3.27
langchain-openai==0.2.14
