make this folder as root to run
create new virtual environment with name "venv"
install requirements from requirements.txt

1. Step 1
run embed.py to create embeddings and store in vector db i.e, chromadb

2. Step 2
run query.py and we can ask a question then script will create embedding of it and does similarity check in vector db then returns chunks